import CounterBox from '@/components/CounterBox';

export default function ReduxPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-zinc-50 p-6 dark:bg-zinc-900">
      <CounterBox />
    </main>
  );
}
